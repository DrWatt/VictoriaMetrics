i- match: '{__name__=~".*_(value|duration)$"}'
  interval: 1h
  outputs:
    - avg
  output_relabel_configs:
    - source_labels: [__name__]
      regex: '(.+):1h_avg'
      target_label: __name__
      replacement: '$1'
